// file: PftNavigatorApplication.kt
package com.logesh.pftnavigator

import android.app.Application

/**
 * The base Application class for the app.
 * Can be used for app-level initializations if needed in the future.
 */
class PftNavigatorApplication : Application() {
    // The repository has been removed, so this class is now empty.
    // We will keep the class itself in case we need it for other initializations later.
}